import './index.css';
const React=(window as any).React;const{useEffect,useMemo,useState}=React;
const CRM=({api,onClose}:any)=>{const[tab,setTab]=useState('overview');const[customers,setCustomers]=useState<any[]>([]);const[leads,setLeads]=useState<any[]>([]);const[appts,setAppts]=useState<any[]>([]);const[invoices,setInvoices]=useState<any[]>([]);const[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);try{const[c,l,a,i]=await Promise.all([api.getCustomers?.()||[],api.getLeads?.()||[],api.getAppointments?.()||[],api.getInvoices?.()||[]]);setCustomers(c||[]);setLeads(l||[]);setAppts(a||[]);setInvoices(i||[])}finally{setLoading(false)}};useEffect(()=>{load()},[]);const openLeads=useMemo(()=>leads.filter((x:any)=>!['won','lost','closed'].includes(String(x.status||'').toLowerCase())),[leads]);const upcoming=useMemo(()=>appts.filter((x:any)=>!['completed','cancelled'].includes(String(x.status||'').toLowerCase())),[appts]);const tabs=[['overview','Overview'],['customers','Customers'],['leads','Pipeline'],['appointments','Appointments'],['billing','Billing'],['activity','Activity'],['reports','Reports']];return <div className="rita-module"><div className="rita-shell"><div className="rita-header"><div><h1 className="rita-title">Service CRM</h1><div className="rita-sub">Customer relationships, leads, appointments and service revenue in one RITA workspace.</div></div><div style={{display:'flex',gap:10}}><button className="rita-btn secondary" onClick={load}>Refresh</button><button className="rita-btn secondary" onClick={onClose}>Close Module</button></div></div><div className="rita-tabs">{tabs.map((t:any)=><div key={t[0]} className={`rita-tab ${tab===t[0]?'active':''}`} onClick={()=>setTab(t[0])}>{t[1]}</div>)}</div>{loading?<div className="rita-empty">Loading CRM data…</div>:<>{tab==='overview'&&<><div className="rita-grid"><div className="rita-card"><div className="rita-kpi-label">Customers</div><div className="rita-kpi">{customers.length}</div></div><div className="rita-card"><div className="rita-kpi-label">Open leads</div><div className="rita-kpi">{openLeads.length}</div></div><div className="rita-card"><div className="rita-kpi-label">Appointments</div><div className="rita-kpi">{upcoming.length}</div></div><div className="rita-card"><div className="rita-kpi-label">Invoices</div><div className="rita-kpi">{invoices.length}</div></div></div><div className="rita-card rita-section"><h3>Pipeline watch</h3><div className="rita-table-wrap"><table className="rita-table"><thead><tr><th>Lead</th><th>Stage</th><th>Value</th></tr></thead><tbody>{openLeads.slice(0,10).map((l:any)=><tr key={l.id}><td>{l.name||l.customerName||l.title||'-'}</td><td><span className="rita-badge">{l.status||l.stage||'Open'}</span></td><td>{l.value??l.amount??'-'}</td></tr>)}</tbody></table></div></div></>}{tab==='customers'&&<div className="rita-card"><h3>Customer Directory</h3><div className="rita-table-wrap"><table className="rita-table"><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Balance</th></tr></thead><tbody>{customers.map((c:any)=><tr key={c.id}><td>{c.name||'-'}</td><td>{c.phone||'-'}</td><td>{c.email||'-'}</td><td>{c.balance??'-'}</td></tr>)}</tbody></table></div></div>}{tab==='leads'&&<div className="rita-card"><h3>Sales Pipeline</h3><div className="rita-table-wrap"><table className="rita-table"><thead><tr><th>Lead</th><th>Source</th><th>Status</th><th>Value</th></tr></thead><tbody>{leads.map((l:any)=><tr key={l.id}><td>{l.name||l.title||'-'}</td><td>{l.source||'-'}</td><td><span className="rita-badge">{l.status||l.stage||'Open'}</span></td><td>{l.value??l.amount??'-'}</td></tr>)}</tbody></table></div></div>}{tab==='appointments'&&<div className="rita-card"><h3>Appointments</h3><div className="rita-table-wrap"><table className="rita-table"><thead><tr><th>Customer</th><th>Service</th><th>Date</th><th>Status</th></tr></thead><tbody>{appts.map((a:any)=><tr key={a.id}><td>{a.customerName||a.customer||'-'}</td><td>{a.service||a.title||'-'}</td><td>{a.date||a.startTime||a.start_time||'-'}</td><td><span className="rita-badge">{a.status||'Scheduled'}</span></td></tr>)}</tbody></table></div></div>}{tab==='billing'&&<div className="rita-card"><h3>Billing & Invoices</h3><div className="rita-table-wrap"><table className="rita-table"><thead><tr><th>Invoice</th><th>Customer</th><th>Status</th><th>Total</th></tr></thead><tbody>{invoices.map((i:any)=><tr key={i.id}><td>{i.number||i.invoiceNo||i.id}</td><td>{i.customerName||i.customer||'-'}</td><td><span className="rita-badge">{i.status||'Open'}</span></td><td>{i.total??i.amount??'-'}</td></tr>)}</tbody></table></div></div>}{tab==='activity'&&<div className="rita-card"><h3>Customer Activity</h3><p className="rita-sub">This workspace combines customer, appointment, pipeline and billing records so teams can follow the full service lifecycle.</p></div>}{tab==='reports'&&<div className="rita-card"><h3>CRM Reports</h3><p className="rita-sub">Track lead conversion, customer activity, appointments and billing from the live CRM datasets.</p></div>}</>}</div></div>};
(window as any).RitaPlugin={mount:(c:any,p:any)=>{const r=(window as any).ReactDOM.createRoot(c);r.render(React.createElement(CRM,p));(window as any).RitaPlugin._root=r},unmount:()=>{(window as any).RitaPlugin._root?.unmount()}};