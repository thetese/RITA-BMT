

=== CHUNK ===
Created At: 2026-07-13T22:05:56Z
Completed At: 2026-07-13T22:05:56Z
File Path: `file:///C:/Users/pc/.gemini/antigravity/brain/9861ae8b-a3a4-40ae-b871-009c39b156e6/task.md`
Total Lines: 23
Total Bytes: 1137
Showing lines 1 to 23
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1: - `[x]` Phase 1: Setup TypeScript & Install Dependencies (Zustand, umzug)
2:   - `[x]` Install packages: `typescript`, `@types/react`, `@types/node`, `zustand`, `umzug`
3:   - `[x]` Create `tsconfig.json`
4:   - `[x]` Rename all `.js`/`.jsx` files to `.ts`/`.tsx`
5: - `[x]` Phase 2: Database Migrations
6:   - `[x]` Setup `umzug` migration runner
7:   - `[x]` Extract existing schema into migration files
8:   - `[x]` Refactor `Store.js` to use migrations
9: - `[x]` Phase 3: Backend IPC Refactoring
10:   - `[x]` Create `salesController.ts`, `userController.ts`, etc.
11:   - `[x]` Refactor `main.ts` to use controllers
12: - `[x]` Phase 4: Frontend State Management (Zustand)
13:   - `[x]` Create Zustand stores (`useAuthStore.ts`, `useSalesStore.ts`)
14:   - `[x]` Refactor `App.tsx` and major components to use Zustand
15: - `[/]` Phase 5: Component Refactoring
16:   - `[ ]` Break down `RestaurantPOS.tsx` and `RetailPOS.tsx`
17: - `[x]` Phase 6: Hardware Integration
18:   - `[x]` Install `escpos` libraries
19:   - `[x]` Implement direct thermal printing service
20: - `[x]` Phase 7: Final Verification
21:   - `[x]` Fix remaining TypeScript compilation errors
22:   - `[x]` Test end-to-end functionality
23: 
The above content shows the entire, complete file contents of the requested file.


=== CHUNK ===
Created At: 2026-07-13T22:16:07Z
Completed At: 2026-07-13T22:16:07Z
File Path: `file:///c:/Users/pc/Desktop/fidele/src/components/RestaurantPOS.tsx`
Total Lines: 816
Total Bytes: 41721
Showing lines 1 to 200
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1: // @ts-nocheck
2: import React, { useState, useEffect, useRef } from 'react';
3: 
4: const playDing = () => {
5:   try {
6:     const AudioContext = window.AudioContext || window.webkitAudioContext;
7:     if (!AudioContext) return;
8:     const ctx = new AudioContext();
9:     const osc = ctx.createOscillator();
10:     const gainNode = ctx.createGain();
11:     osc.connect(gainNode);
12:     gainNode.connect(ctx.destination);
13:     osc.type = 'triangle';
14:     osc.frequency.setValueAtTime(1046.5, ctx.currentTime);
15:     osc.frequency.setValueAtTime(1318.5, ctx.currentTime + 0.1);
16:     gainNode.gain.setValueAtTime(0.1, ctx.currentTime);
17:     gainNode.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.5);
18:     osc.start();
19:     osc.stop(ctx.currentTime + 0.5);
20:   } catch(e) {}
21: };
22: import { 
23:   Printer, Banknote, Smartphone, CreditCard, Search, X, Trash2, Mic, PauseCircle,
24:   ShoppingCart, FolderOpen, Save, Plus, ArrowLeft, ChefHat
25: } from 'lucide-react';
26: import { v4 as uuidv4 } from 'uuid';
27: import { generateThermalReceiptHTML, generateProformaHTML } from '../utils/receiptGenerator';
28: import { vsdcApi } from '../utils/vsdcMock';
29: import ShiftManager from './ShiftManager';
30: import POSLayout from './ui/POSLayout';
31: import Modal from './ui/Modal';
32: import { useToast } from './ui/Toast';
33: import { useConfirm } from './ui/Confirm';
34: import useBarcodeScanner from '../hooks/useBarcodeScanner';
35: import usePOSCart from '../hooks/usePOSCart';
36: import useHeldCarts from '../hooks/useHeldCarts';
37: import { buildVSDCPayload } from '../utils/vsdc';
38: import { formatMoney } from '../utils/format';
39: import CartItem from './ui/CartItem';
40: import FloorPlanSelector from './FloorPlanSelector';
41: import SplitCheckModal from './SplitCheckModal';
42: 
43: export default function RestaurantPOS({ currentUser, sales = [], crmCustomers: initialCrmCustomers = [], onSave }) {
44:   const { showToast } = useToast();
45:   const { askConfirm } = useConfirm();
46: 
47:   const [waiters, setWaiters] = useState([]);
48:   const [selectedWaiter, setSelectedWaiter] = useState(null);
49:   const [view, setView] = useState('waiterSelect'); // 'waiterSelect', 'dashboard', 'pos'
50:   
51:   const [products, setProducts] = useState([]);
52:   const [categories, setCategories] = useState([]);
53:   const [filterCategory, setFilterCategory] = useState('');
54:   const [search, setSearch] = useState('');
55:   
56:   const [paymentMethod, setPaymentMethod] = useState('Cash');
57:   const [customerName, setCustomerName] = useState('');
58:   const [crmCustomers, setCrmCustomers] = useState([]);
59:   const [selectedCustomer, setSelectedCustomer] = useState('');
60:   const [redeemPoints, setRedeemPoints] = useState(0);
61:   const [notes, setNotes] = useState('');
62:   const [paymentDetails, setPaymentDetails] = useState({ Cash: 0, Card: 0, Momo: 0, Credit: 0 });
63:   const [showFloorPlan, setShowFloorPlan] = useState(false);
64:   
65:   const [showHeldModal, setShowHeldModal] = useState(false);
66:   const [showPaymentModal, setShowPaymentModal] = useState(false);
67:   const [showSplitModal, setShowSplitModal] = useState(false);
68: 
69:   const [showTableModal, setShowTableModal] = useState(false);
70:   const [tableNameInput, setTableNameInput] = useState('');
71:   const [tables, setTables] = useState([]);
72: 
73:   const [activeShift, setActiveShift] = useState(null);
74:   const [shiftMode, setShiftMode] = useState(null);
75:   const [momoPhone, setMomoPhone] = useState('');
76:   const [isProcessingMomo, setIsProcessingMomo] = useState(false);
77:   const notifiedReadyItemsRef = useRef(new Set());
78: 
79:   // Initialize Hooks
80:   const { cart, setCart, addToCart, updateQuantity, updateDiscount, calculateItemDiscount, totalAmount, clearCart } = usePOSCart(products);
81:   const { heldCarts, activeOrderId, activeOrderName, loadHeldCarts, saveHeldCart, restoreCart, deleteHeldCart, clearActiveOrder } = useHeldCarts(window.api);
82: 
83:   // Enable barcode scanner
84:   useBarcodeScanner(products, addToCart, view === 'pos', (msg) => showToast(msg, 'error'));
85: 
86:   const loadData = async () => {
87:     if (!window.api) return;
88:     const data = await window.api.getProducts();
89:     setProducts(data);
90:     const wData = await window.api.getAccounters();
91:     setWaiters(wData);
92:     await loadHeldCarts();
93:     const cData = await window.api.getCustomers();
94:     setCrmCustomers(cData);
95:     const tData = await window.api.getTables();
96:     setTables(tData);
97:     const prods = await window.api.getProducts();
98:     setProducts(prods.filter(p => p.stockQuantity > 0 || p.stockQuantity === ''));
99:   };
100: 
101:   const loadShift = async () => {
102:     const shift = await window.api.getActiveShift(currentUser.id);
103:     setActiveShift(shift);
104:   };
105: 
106:   useEffect(() => { loadData(); }, []);
107:   useEffect(() => { if (currentUser) loadShift(); }, [currentUser]);
108: 
109:   useEffect(() => {
110:     let removeListener = null;
111:     if (window.api && window.api.onCloudUpdate) {
112:       removeListener = window.api.onCloudUpdate(async (data) => {
113:         if (data && data.table === 'held_carts') {
114:           const freshCarts = await loadHeldCarts();
115:           let hasNewReady = false;
116:           const currentNotified = notifiedReadyItemsRef.current;
117:           
118:           if (freshCarts) {
119:             freshCarts.forEach(cart => {
120:               try {
121:                 const items = JSON.parse(cart.cartData);
122:                 items.forEach(item => {
123:                   const uniqueId = `${cart.id}-${item.productId}`;
124:                   if (item.status === 'ready' && !currentNotified.has(uniqueId)) {
125:                     hasNewReady = true;
126:                     currentNotified.add(uniqueId);
127:                   }
128:                 });
129:               } catch (e) {}
130:             });
131:           }
132: 
133:           if (hasNewReady) {
134:             playDing();
135:             showToast("Food is ready in the kitchen!", "success");
136:           }
137:         }
138:       });
139:     }
140:     return () => {
141:       if (removeListener) removeListener();
142:     };
143:   }, [loadHeldCarts]);
144: 
145:   const handleOpenShift = async (cash) => {
146:     await window.api.openShift(currentUser.id, cash);
147:     await loadShift();
148:   };
149: 
150:   const handleCloseShift = async (cash) => {
151:     await window.api.closeShift(activeShift.id, cash);
152:     setActiveShift(null);
153:     setShiftMode(null);
154:   };
155: 
156:   if (currentUser?.id && !activeShift) return <ShiftManager mode="open" onSubmit={handleOpenShift} />;
157:   if (shiftMode === 'close' && activeShift) return <ShiftManager mode="close" shift={activeShift} onSubmit={handleCloseShift} onCancel={() => setShiftMode(null)} />;
158: 
159:   const handleHoldCartClick = async () => {
160:     if (cart.length === 0) return;
161:     try {
162:       if (activeOrderId) {
163:         await saveHeldCart(cart, activeOrderName, currentUser.username);
164:         clearCart();
165:         setCustomerName('');
166:         setNotes('');
167:         clearActiveOrder();
168:         await loadData();
169:         showToast("Table/Order saved successfully", "success");
170:       } else {
171:         setTableNameInput(customerName || `Table-${Date.now().toString().slice(-4)}`);
172:         setShowTableModal(true);
173:       }
174:     } catch (err) {
175:       showToast("Error saving order: " + err.message, "error");
176:     }
177:   };
178: 
179:   const confirmSaveTable = async () => {
180:     if (!tableNameInput.trim()) {
181:       showToast("Please enter a name or table for the order.", "error");
182:       return;
183:     }
184:     try {
185:       await saveHeldCart(cart, tableNameInput, currentUser.username);
186:       setShowTableModal(false);
187:       clearCart();
188:       setCustomerName('');
189:       setNotes('');
190:       clearActiveOrder();
191:       await loadData();
192:       showToast("Table/Order saved successfully", "success");
193:     } catch (err) {
194:       showToast("Error saving order: " + err.message, "error");
195:     }
196:   };
197: 
198:   const handleMomoPay = async () => {
199:     if (!momoPhone || momoPhone.length < 9) {
200:       showToast("Please enter a valid phone number", "error");
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.


=== CHUNK ===
Created At: 2026-07-13T22:16:21Z
Completed At: 2026-07-13T22:16:21Z
File Path: `file:///c:/Users/pc/Desktop/fidele/src/components/RestaurantPOS.tsx`
Total Lines: 816
Total Bytes: 41721
Showing lines 200 to 400
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
200:       showToast("Please enter a valid phone number", "error");
201:       return;
202:     }
203:     setIsProcessingMomo(true);
204:     showToast(`MoMo USSD Push sent to ${momoPhone}. Waiting for customer to confirm PIN...`, "success");
205:     
206:     // Simulate API delay for customer to enter PIN
207:     setTimeout(() => {
208:       setIsProcessingMomo(false);
209:       if (selectedCustomer) {
210:         const custInfo = crmCustomers.find(c => c.id === selectedCustomer);
211:         if (custInfo && custInfo.points && redeemPoints > custInfo.points) setRedeemPoints(custInfo.points);
212:       }
213:       setPaymentDetails({ Cash: 0, Card: 0, Momo: finalTotalAmount, Credit: 0 });
214:       setShowPaymentModal(true);
215:       showToast("MoMo Payment Successful!", "success");
216:     }, 4000);
217:   };
218: 
219:   const handlePrintProforma = async () => {
220:     if (cart.length === 0) return;
221:     try {
222:       const receiptCart = cart.map(item => ({ ...item, discountAmount: calculateItemDiscount(item) }));
223:       const businessName = await window.api.getSetting('businessName') || '';
224:       const businessAddress = await window.api.getSetting('businessAddress') || '';
225:       const businessPhone = await window.api.getSetting('businessPhone') || '';
226:       
227:       const htmlReceipt = generateProformaHTML(
228:         receiptCart, totalAmount, customerName, currentUser.username,
229:         { businessName, businessAddress, businessPhone }, activeOrderName
230:       );
231:       const printerName = await window.api.getSetting('receiptPrinter');
232:       const printResult = await window.api.printReceipt(htmlReceipt, printerName || '');
233:       
234:       if (printResult.success) {
235:         showToast('Proforma bill printed successfully.', 'success');
236:       } else {
237:         showToast('Failed to print bill: ' + (printResult.errorType || 'Unknown error'), 'error');
238:       }
239:     } catch (err) {
240:       showToast("Error printing bill: " + err.message, "error");
241:     }
242:   };
243: 
244:   const finalTotalAmount = Math.max(0, totalAmount - (redeemPoints * 10));
245:   const totalPaid = (parseFloat(paymentDetails.Cash) || 0) + (parseFloat(paymentDetails.Card) || 0) + (parseFloat(paymentDetails.Momo) || 0) + (parseFloat(paymentDetails.Credit) || 0);
246:   const changeDue = totalPaid - finalTotalAmount;
247: 
248:   const handlePrintBill = async () => {
249:     if (cart.length === 0) return;
250:     try {
251:       const receiptCart = cart.map(item => ({ ...item, discountAmount: calculateItemDiscount(item) }));
252:       const businessName = await window.api.getSetting('businessName') || '';
253:       const businessAddress = await window.api.getSetting('businessAddress') || '';
254:       const businessPhone = await window.api.getSetting('businessPhone') || '';
255:       
256:       const htmlReceipt = generateProformaHTML(
257:         receiptCart, totalAmount, customerName, currentUser.username,
258:         { businessName, businessAddress, businessPhone }, activeOrderName
259:       );
260: 
261:       const printerName = await window.api.getSetting('receiptPrinter');
262:       const printResult = await window.api.printReceipt(htmlReceipt, printerName || '');
263:       
264:       if (!printResult.success) showToast('Failed to print bill: ' + (printResult.errorType || 'Unknown error'), 'error');
265:     } catch (err) {
266:       showToast("Error printing bill: " + err.message, "error");
267:     }
268:   };
269: 
270:   const handleCheckout = async () => {
271:     if (totalPaid < finalTotalAmount) {
272:       showToast("Insufficient payment amount.", "error");
273:       return;
274:     }
275: 
276:     try {
277:       const receiptId = uuidv4();
278:       const dateStr = new Date().toISOString().split('T')[0];
279:       
280:       const tin = await window.api.getSetting('tin') || "999999999";
281:       const businessName = await window.api.getSetting('businessName') || '';
282:       const businessAddress = await window.api.getSetting('businessAddress') || '';
283:       const businessPhone = await window.api.getSetting('businessPhone') || '';
284:       
285:       const customerInfo = selectedCustomer ? crmCustomers.find(c => c.id === selectedCustomer) : null;
286:       
287:       const creditAmount = parseFloat(paymentDetails.Credit) || 0;
288:       if (creditAmount > 0) {
289:         if (!customerInfo) {
290:           showToast('You must select a registered customer to use Store Credit.', 'error');
291:           return;
292:         }
293:         if (!customerInfo.creditLimit || customerInfo.creditLimit <= 0) {
294:           showToast('This customer is not approved for store credit.', 'error');
295:           return;
296:         }
297:         const currentBalance = customerInfo.accountBalance || 0;
298:         if ((currentBalance + creditAmount) > customerInfo.creditLimit) {
299:           showToast(`Credit limit exceeded! Customer can only borrow up to ${(customerInfo.creditLimit - currentBalance).toLocaleString()} FRW more.`, 'error');
300:           return;
301:         }
302:       }
303:       
304:       const { rceipt, taxblAmtA, taxblAmtB, taxAmtB, itemList } = buildVSDCPayload(cart, paymentDetails, tin, totalAmount, calculateItemDiscount, receiptId, customerInfo);
305: 
306:       rceipt.pmtTyCd = paymentDetails.Cash >= paymentDetails.Card && paymentDetails.Cash >= paymentDetails.Momo ? "01" : paymentDetails.Card >= paymentDetails.Momo ? "02" : "04";
307:       rceipt.salesSttsCd = "02";
308:       rceipt.salesTyCd = "N";
309: 
310:       const vsdcResponse = await vsdcApi.saveSales(rceipt);
311:       const rcptSign = vsdcResponse.data.rcptSign;
312:       const intrlData = vsdcResponse.data.intrlData;
313:       const rcptNo = vsdcResponse.data.rcptNo;
314: 
315:       // 1. Add each sale to DB
316:       for (const item of cart) {
317:         const dcAmt = calculateItemDiscount(item);
318:         const dcRt = item.discount.includes('%') ? parseFloat(item.discount) || 0 : 0;
319:         const finalPrice = (item.quantity * item.unitPrice) - dcAmt;
320: 
321:         await window.api.addSale({
322:           productId: item.originalProductId || item.productId,
323:           productName: item.productName,
324:           category: item.category,
325:           quantity: item.quantity,
326:           unitPrice: item.unitPrice,
327:           costPrice: item.costPrice,
328:           totalPrice: finalPrice - (redeemPoints > 0 ? (redeemPoints * 10 / cart.length) : 0),
329:           date: dateStr,
330:           customerName: customerInfo ? customerInfo.name : '',
331:           customerId: selectedCustomer || null,
332:           notes: notes,
333:           paymentMethod: paymentDetails.Cash >= paymentDetails.Card && paymentDetails.Cash >= paymentDetails.Momo && paymentDetails.Cash >= paymentDetails.Credit ? 'Cash' 
334:                          : paymentDetails.Card >= paymentDetails.Momo && paymentDetails.Card >= paymentDetails.Credit ? 'Card' 
335:                          : paymentDetails.Credit >= paymentDetails.Momo ? 'Store Credit' : 'Mobile Money',
336:           paymentDetails: JSON.stringify({
337:             Cash: parseFloat(paymentDetails.Cash) || 0,
338:             Card: parseFloat(paymentDetails.Card) || 0,
339:             "Mobile Money": parseFloat(paymentDetails.Momo) || 0,
340:             "Store Credit": parseFloat(paymentDetails.Credit) || 0
341:           }),
342:           discountAmount: dcAmt,
343:           discountRate: dcRt,
344:           receiptId: receiptId,
345:           receiptSignature: rcptSign,
346:           internalData: intrlData,
347:           receiptNo: rcptNo,
348:           waiterName: selectedWaiter?.name
349:         }, currentUser.id);
350:       }
351: 
352:       if (redeemPoints > 0 && selectedCustomer) {
353:         await window.api.deductCustomerPoints(selectedCustomer, redeemPoints);
354:       }
355:       if (creditAmount > 0 && selectedCustomer) {
356:         await window.api.adjustCustomerBalance(selectedCustomer, creditAmount);
357:       }
358: 
359:       // 2. Generate Receipt HTML
360:       const receiptCart = cart.map(item => ({ ...item, discountAmount: calculateItemDiscount(item) }));
361: 
362:       const htmlReceipt = generateThermalReceiptHTML(
363:         receiptCart, totalAmount, receiptId, paymentMethod, customerName, selectedWaiter?.name,
364:         {
365:           tin, businessName, businessAddress, businessPhone, rcptSign, intrlData, rcptNo,
366:           sdcId: vsdcResponse.data.sdcId, mrcNo: vsdcResponse.data.mrcNo, taxblAmtA, taxblAmtB, taxAmtB
367:         },
368:         {
369:           Cash: parseFloat(paymentDetails.Cash) || 0, Card: parseFloat(paymentDetails.Card) || 0, "Mobile Money": parseFloat(paymentDetails.Momo) || 0, "Store Credit": parseFloat(paymentDetails.Credit) || 0
370:         }
371:       );
372: 
373:       // 3. Print
374:       const printerName = await window.api.getSetting('receiptPrinter');
375:       const printResult = await window.api.printReceipt(htmlReceipt, printerName || '');
376:       
377:       if (printResult.success) {
378:         showToast('Checkout complete and receipt printed!', 'success');
379:       } else {
380:         showToast('Checkout complete, but printing failed: ' + (printResult.errorType || 'Unknown error'), 'error');
381:       }
382: 
383:       // 4. Clear state
384:       if (activeOrderId) await deleteHeldCart(activeOrderId, () => true);
385:       clearCart();
386:       setCustomerName('');
387:       setNotes('');
388:       setPaymentDetails({ Cash: 0, Card: 0, Momo: 0, Credit: 0 });
389:       setShowPaymentModal(false);
390:       clearActiveOrder();
391:       if (onSave) onSave();
392:       setView('waiterSelect');
393:       setSelectedWaiter(null);
394:       await loadData();
395:       if (onSave) onSave();
396: 
397:     } catch (err) {
398:       showToast("Error during checkout: " + err.message, "error");
399:     }
400:   };
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.


=== CHUNK ===
Created At: 2026-07-13T22:16:30Z
Completed At: 2026-07-13T22:16:30Z
File Path: `file:///c:/Users/pc/Desktop/fidele/src/components/RestaurantPOS.tsx`
Total Lines: 816
Total Bytes: 41721
Showing lines 500 to 816
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
500:                 name: tableName,
501:                 cartData: emptyCartStr,
502:                 waiterName: selectedWaiter?.name || currentUser.username
503:               });
504:               await loadHeldCarts();
505:               await restoreCart(newCart, [], () => true);
506:               setShowFloorPlan(false);
507:               setView('pos');
508:             }} 
509:             onClose={() => setShowFloorPlan(false)} 
510:           />
511:         </div>
512:       )}
513:       </>
514:     );
515:   }
516: 
517:   const LeftPanel = (
518:     <>
519:       <div className="pos-search-bar">
520:         <input 
521:           type="text" 
522:           placeholder="Search products..." 
523:           value={search} 
524:           onChange={e => setSearch(e.target.value)} 
525:           className="pos-search-input"
526:         />
527:         <select value={filterCategory} onChange={e => setFilterCategory(e.target.value)} className="pos-search-input" style={{ flex: '0 0 200px' }}>
528:           <option value="">All Categories</option>
529:           {categories.map(c => <option key={c} value={c}>{c}</option>)}
530:         </select>
531:       </div>
532:       <div className="pos-product-grid">
533:         {filteredProducts.map(p => {
534:           const isOutOfStock = p.stockQuantity <= 0;
535:           const isLowStock = p.stockQuantity > 0 && p.stockQuantity <= 5;
536:           const catHash = p.category ? p.category.charCodeAt(0) % 5 : 0;
537:           const cardBg = isOutOfStock ? 'var(--bg-secondary)' : `var(--pos-cat-${catHash})`;
538: 
539:           return (
540:             <div 
541:               key={p.id} 
542:               onClick={() => { if (!isOutOfStock) addToCart(p, null); }}
543:               className={`pos-product-card ${isOutOfStock ? 'out-of-stock' : ''}`}
544:               style={{ background: cardBg }}
545:             >
546:               {isLowStock && <div style={{ position: 'absolute', top: '-8px', right: '-8px', background: '#ff9800', color: '#fff', fontSize: '0.7rem', padding: '2px 6px', borderRadius: '10px', fontWeight: 'bold', boxShadow: '0 2px 4px rgba(0,0,0,0.2)', zIndex: 10 }}>LOW STOCK</div>}
547:               <div>
548:                 <div style={{ display: 'inline-block', padding: '2px 6px', background: 'var(--pos-cat-tag-bg)', borderRadius: '4px', fontSize: '0.65rem', fontWeight: 'bold', textTransform: 'uppercase', color: 'var(--pos-cat-tag-text)', marginBottom: '4px', maxWidth: '100%', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
549:                   {p.category || 'Item'}
550:                 </div>
551:                 <div className="pos-product-title" style={{ color: 'var(--pos-card-title)' }}>{p.productName}</div>
552:               </div>
553:               <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-start', marginTop: '6px' }}>
554:                 <div style={{ fontSize: '0.7rem', color: isLowStock ? '#ff9800' : 'var(--text-secondary)', fontWeight: isLowStock ? 'bold' : 'normal', marginBottom: '2px' }}>
555:                   Stock: {p.stockQuantity || 0}
556:                 </div>
557:                 <div className="pos-product-price" style={{ whiteSpace: 'nowrap' }}>{formatMoney(p.unitPrice)} FRW</div>
558:               </div>
559:             </div>
560:           );
561:         })}
562:         {filteredProducts.length === 0 && (
563:           <div style={{ gridColumn: '1 / -1', textAlign: 'center', color: 'var(--text-secondary)', padding: '40px', fontSize: '1.2rem' }}>No products found.</div>
564:         )}
565:       </div>
566:     </>
567:   );
568: 
569:   const RightPanel = (
570:     <>
571:       <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', margin: '0 0 20px 0' }}>
572:         <h2 style={{ margin: 0, fontSize: '1.5rem' }}>{activeOrderId ? `Table: ${activeOrderName}` : 'New Order'}</h2>
573:         <div style={{ display: 'flex', gap: '8px' }}>
574:           <button className="btn-secondary btn-sm" onClick={() => setView('dashboard')} style={{ padding: '8px 12px', display: 'flex', alignItems: 'center', gap: '6px' }}>
575:             <ArrowLeft size={16} /> Home
576:           </button>
577:           <button className="btn-secondary btn-sm" onClick={handleHoldCartClick} disabled={cart.length === 0} style={{ padding: '8px 12px', display: 'flex', alignItems: 'center', gap: '6px', background: 'var(--primary)', color: 'white', border: 'none' }}>
578:             {activeOrderId ? <><ChefHat size={16} /> Update Kitchen</> : <><ChefHat size={16} /> Send to Kitchen</>}
579:           </button>
580:         </div>
581:       </div>
582:       
583:       <div className="pos-cart-list">
584:         {cart.map(item => {
585:           const dcAmt = calculateItemDiscount(item);
586:           return (
587:             <CartItem 
588:               key={item.productId} 
589:               item={item} 
590:               dcAmt={dcAmt} 
591:               onUpdateQuantity={updateQuantity} 
592:               onUpdateDiscount={updateDiscount} 
593:             />
594:           );
595:         })}
596:         {cart.length === 0 && (
597:           <div style={{ textAlign: 'center', color: 'var(--text-secondary)', marginTop: '60px', fontSize: '1.1rem', display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '10px' }}>
598:             <ShoppingCart size={48} style={{ opacity: 0.3 }} /> Cart is empty
599:           </div>
600:         )}
601:       </div>
602: 
603:       <div className="pos-checkout-area">
604:         <div style={{ display: 'grid', gridTemplateColumns: '1fr', gap: '15px', marginBottom: '20px' }}>
605:           <div className="form-row">
606:             <label style={{ display: 'block', marginBottom: '5px' }}>Customer</label>
607:             <select value={selectedCustomer} onChange={(e) => setSelectedCustomer(e.target.value)} className="pos-search-input" style={{ padding: '10px 16px' }}>
608:               <option value="">Walk-in Customer</option>
609:               {crmCustomers.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}
610:             </select>
611:           </div>
612:         </div>
613: 
614:         <div style={{ display: 'flex', flexDirection: 'column', gap: '12px', marginBottom: '20px' }}>
615:           <input type="text" placeholder="Notes (Optional)" value={notes} onChange={e => setNotes(e.target.value)} className="pos-search-input" style={{ padding: '10px 16px' }} />
616:           
617:           {selectedCustomer && crmCustomers.find(c => c.id === selectedCustomer)?.points > 0 && (
618:             <div style={{ display: 'flex', gap: '10px', alignItems: 'center', background: 'rgba(79, 70, 229, 0.05)', padding: '12px', borderRadius: '12px', border: '1px dashed var(--primary)' }}>
619:               <span style={{ fontSize: '0.85rem', color: 'var(--primary)', flex: 1, fontWeight: '600' }}>
620:                 Redeem Points (Max {crmCustomers.find(c => c.id === selectedCustomer).points})<br/>
621:                 <small style={{ opacity: 0.8 }}>1 pt = 10 FRW off</small>
622:               </span>
623:               <input type="number" max={crmCustomers.find(c => c.id === selectedCustomer).points} min="0" value={redeemPoints} onChange={e => setRedeemPoints(Math.min(parseInt(e.target.value) || 0, crmCustomers.find(c => c.id === selectedCustomer).points))} style={{ padding: '8px', borderRadius: '8px', border: '1px solid var(--primary)', width: '80px', textAlign: 'center', fontWeight: 'bold' }} />
624:             </div>
625:           )}
626:         </div>
627:         <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '20px' }}>
628:           <span style={{ fontSize: '1.25rem', fontWeight: '700', color: 'var(--text-secondary)' }}>Total</span>
629:           <span className="pos-gradient-text">{formatMoney(finalTotalAmount)} FRW</span>
630:         </div>
631:         <div style={{ display: 'flex', gap: '12px' }}>
632:           <button className="btn-secondary" style={{ flex: '0 0 120px', padding: '16px', borderRadius: '16px', fontWeight: 'bold', border: '2px solid var(--border-color)' }} onClick={handlePrintBill} disabled={cart.length === 0}>
633:             <Printer size={18} style={{ marginRight: '6px' }} /> Bill
634:           </button>
635:           <button className="btn-secondary" style={{ flex: '1', padding: '16px', borderRadius: '16px', fontWeight: 'bold', fontSize: '1.1rem', border: '2px solid var(--border-color)' }} onClick={() => setShowSplitModal(true)} disabled={cart.length === 0}>
636:             Split
637:           </button>
638:           <button className="pos-checkout-btn" style={{ flex: '1' }} onClick={() => { setPaymentDetails({ Cash: 0, Card: 0, Momo: 0, Credit: 0 }); setShowPaymentModal(true); }} disabled={cart.length === 0}>
639:             Pay
640:           </button>
641:         </div>
642:       </div>
643:     </>
644:   );
645: 
646:   return (
647:     <>
648:       <POSLayout leftPanel={LeftPanel} rightPanel={RightPanel} />
649: 
650:       {showTableModal && (
651:         <div style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, background: 'rgba(0,0,0,0.6)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 9999 }}>
652:           <FloorPlanSelector 
653:             tables={tables} 
654:             onSelectTable={async (name) => {
655:               setTableNameInput(name);
656:               try {
657:                 await saveHeldCart(cart, name, currentUser.username);
658:                 setShowTableModal(false);
659:                 clearCart();
660:                 setCustomerName('');
661:                 setNotes('');
662:                 clearActiveOrder();
663:                 await loadData();
664:                 showToast("Table/Order saved successfully", "success");
665:               } catch (err) {
666:                 showToast("Error saving order: " + err.message, "error");
667:               }
668:             }} 
669:             onClose={() => setShowTableModal(false)}
670:           />
671:         </div>
672:       )}
673: 
674:       {showSplitModal && (
675:         <SplitCheckModal 
676:           cart={cart}
677:           onClose={() => setShowSplitModal(false)}
678:           onConfirmSplit={async (splitConfig) => {
679:             setShowSplitModal(false);
680:             try {
681:               const baseName = activeOrderName || `Order-${Date.now().toString().slice(-4)}`;
682:               
683:               // 1. Create new held carts for each subCart
684:               for (let i = 0; i < splitConfig.subCarts.length; i++) {
685:                 const subCart = splitConfig.subCarts[i];
686:                 const partName = `${baseName} - Part ${i + 1}`;
687:                 await window.api.addHeldCart({ 
688:                   name: partName, 
689:                   cartData: JSON.stringify(subCart), 
690:                   waiterName: selectedWaiter?.name || currentUser.username 
691:                 });
692:               }
693: 
694:               // 2. Delete the original active order if it existed
695:               if (activeOrderId) {
696:                 await deleteHeldCart(activeOrderId, () => true);
697:               }
698: 
699:               // 3. Clear current cart and reload held carts
700:               clearCart();
701:               clearActiveOrder();
702:               await loadHeldCarts();
703:               
704:               // 4. Navigate to dashboard to see the newly split open tables
705:               setView('dashboard');
706: 
707:               showToast(`Order split into ${splitConfig.subCarts.length} parts. Select a part from Held Orders to checkout.`, 'success');
708:             } catch (err) {
709:               showToast("Error splitting order: " + err.message, "error");
710:             }
711:           }}
712:         />
713:       )}
714: 
715:       <Modal title="Payment" isOpen={showPaymentModal} onClose={() => setShowPaymentModal(false)}>
716:         <div style={{ fontSize: '1.5rem', fontWeight: 'bold', textAlign: 'center', margin: '20px 0' }}>
717:           Total: {formatMoney(finalTotalAmount)} FRW
718:         </div>
719:         
720:         <div style={{ display: 'flex', gap: '10px', marginBottom: '20px' }}>
721:           <button className="btn-secondary" style={{ flex: 1, padding: '8px', fontSize: '0.9rem', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '6px' }} onClick={() => setPaymentDetails({ Cash: finalTotalAmount, Card: 0, Momo: 0, Credit: 0 })}>
722:             <Banknote size={16} /> All Cash
723:           </button>
724:           <button className="btn-secondary" style={{ flex: 1, padding: '8px', fontSize: '0.9rem', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '6px' }} onClick={() => setPaymentDetails({ Cash: 0, Card: 0, Momo: finalTotalAmount, Credit: 0 })}>
725:             <Smartphone size={16} /> All MoMo
726:           </button>
727:           <button className="btn-secondary" style={{ flex: 1, padding: '8px', fontSize: '0.9rem', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '6px' }} onClick={() => setPaymentDetails({ Cash: 0, Card: finalTotalAmount, Momo: 0, Credit: 0 })}>
728:             <CreditCard size={16} /> All Card
729:           </button>
730:           {selectedCustomer && (
731:             <button className="btn-secondary" style={{ flex: 1, padding: '8px', fontSize: '0.9rem', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '6px', background: 'var(--primary)', color: 'white', border: 'none' }} onClick={() => setPaymentDetails({ Cash: 0, Card: 0, Momo: 0, Credit: finalTotalAmount })}>
732:               All Credit
733:             </button>
734:           )}
735:         </div>
736: 
737:         <div style={{ marginBottom: '20px', padding: '15px', background: 'var(--bg-secondary)', borderRadius: '8px', border: '1px solid var(--border-color)' }}>
738:           <h4 style={{ margin: '0 0 10px 0', fontSize: '1rem' }}>MTN Mobile Money Push</h4>
739:           <div style={{ display: 'flex', gap: '10px' }}>
740:             <input 
741:               type="text" 
742:               placeholder="e.g. 078xxxxxxx" 
743:               value={momoPhone || ''} 
744:               onChange={e => setMomoPhone(e.target.value)} 
745:               style={{ flex: 1, padding: '10px', borderRadius: '6px', border: '1px solid var(--border-color)' }}
746:               disabled={isProcessingMomo}
747:             />
748:             <button 
749:               className="btn-primary" 
750:               style={{ background: '#f5a623', color: '#000', fontWeight: 'bold' }} 
751:               onClick={handleMomoPay}
752:               disabled={isProcessingMomo}
753:             >
754:               {isProcessingMomo ? 'Processing...' : 'Send Prompt'}
755:             </button>
756:           </div>
757:         </div>
758: 
759:         <div style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginBottom: '20px' }}>
760:           <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
761:             <label style={{ fontWeight: 'bold' }}>Cash (FRW)</label>
762:             <input type="number" min="0" value={paymentDetails.Cash} onChange={e => setPaymentDetails({...paymentDetails, Cash: e.target.value})} style={{ padding: '8px', borderRadius: '4px', border: '1px solid var(--border-color)', width: '150px', textAlign: 'right' }} />
763:           </div>
764:           <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
765:             <label style={{ fontWeight: 'bold' }}>Card (FRW)</label>
766:             <input type="number" min="0" value={paymentDetails.Card} onChange={e => setPaymentDetails({...paymentDetails, Card: e.target.value})} style={{ padding: '8px', borderRadius: '4px', border: '1px solid var(--border-color)', width: '150px', textAlign: 'right' }} />
767:           </div>
768:           {selectedCustomer && (
769:             <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '1px solid var(--border-color)', paddingBottom: '10px' }}>
770:               <label style={{ fontWeight: 'bold' }}>Store Credit (FRW)</label>
771:               <input type="number" min="0" value={paymentDetails.Credit} onChange={e => setPaymentDetails({...paymentDetails, Credit: e.target.value})} style={{ padding: '8px', borderRadius: '4px', border: '1px solid var(--border-color)', width: '150px', textAlign: 'right' }} />
772:             </div>
773:           )}
774:           <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
775:             <label style={{ fontWeight: 'bold' }}>Momo (FRW)</label>
776:             <input type="number" min="0" value={paymentDetails.Momo} onChange={e => setPaymentDetails({...paymentDetails, Momo: e.target.value})} style={{ padding: '8px', borderRadius: '4px', border: '1px solid var(--border-color)', width: '150px', textAlign: 'right' }} />
777:           </div>
778:         </div>
779: 
780:         <div style={{ padding: '15px', background: changeDue >= 0 ? 'var(--success)' : 'var(--danger)', color: '#fff', borderRadius: '8px', marginBottom: '20px', display: 'flex', justifyContent: 'space-between', fontWeight: 'bold' }}>
781:           <span>{changeDue >= 0 ? 'Change Due:' : 'Remaining Balance:'}</span>
782:           <span>{formatMoney(Math.abs(changeDue))} FRW</span>
783:         </div>
784: 
785:         <div style={{ display: 'flex', gap: '10px' }}>
786:           <button className="btn-secondary" style={{ flex: 1 }} onClick={() => setShowPaymentModal(false)}>Cancel</button>
787:           <button className="btn-primary" style={{ flex: 1 }} onClick={handleCheckout} disabled={totalPaid < finalTotalAmount}>Complete Sale</button>
788:         </div>
789:       </Modal>
790: 
791:       {showFloorPlan && (
792:         <div style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, background: 'rgba(0,0,0,0.8)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 9999 }}>
793:           <FloorPlanSelector 
794:             tables={tables} 
795:             onSelectTable={async (tableName) => {
796:               // Create an empty held cart for this table immediately
797:               const emptyCartStr = JSON.stringify([]);
798:               const newCart = await window.api.addHeldCart({
799:                 name: tableName,
800:                 cartData: emptyCartStr,
801:                 waiterName: selectedWaiter?.name || currentUser.username
802:               });
803:               await loadHeldCarts();
804:               // Mock the cart array to bypass warning
805:               await restoreCart(newCart, [], () => true);
806:               setShowFloorPlan(false);
807:               setView('pos');
808:             }} 
809:             onClose={() => setShowFloorPlan(false)} 
810:           />
811:         </div>
812:       )}
813:     </>
814:   );
815: }
816: 
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.


=== CHUNK ===
Created At: 2026-07-13T22:20:09Z
Completed At: 2026-07-13T22:20:09Z
File Path: `file:///c:/Users/pc/Desktop/fidele/src/components/RestaurantPOS.tsx`
Total Lines: 54
Total Bytes: 2043
Showing lines 1 to 54
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1: // @ts-nocheck
2: import React, { useState, useEffect, useRef } from 'react';
3: 
4: const playDing = () => {
5:   try {
6:     const AudioContext = window.AudioContext || window.webkitAudioContext;
7:     if (!AudioContext) return;
8:     const ctx = new AudioContext();
9:     const osc = ctx.createOscillator();
10:     const gainNode = ctx.createGain();
11:     osc.connect(gainNode);
12:     gainNode.connect(ctx.destination);
13:     osc.type = 'triangle';
14:     osc.frequency.setValueAtTime(1046.5, ctx.currentTime);
15:     osc.frequency.setValueAtTime(1318.5, ctx.currentTime + 0.1);
16:     gainNode.gain.setValueAtTime(0.1, ctx.currentTime);
17:     gainNode.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.5);
18:     osc.start();
19:     osc.stop(ctx.currentTime + 0.5);
20:   } catch(e) {}
21: };
22: import { 
23:   Printer, Banknote, Smartphone, CreditCard, Search, X, Trash2, Mic, PauseCircle,
24:   ShoppingCart, FolderOpen, Save, Plus, ArrowLeft, ChefHat
25: } from 'lucide-react';
26: import { v4 as uuidv4 } from 'uuid';
27: import { generateThermalReceiptHTML, generateProformaHTML } from '../utils/receiptGenerator';
28: import { vsdcApi } from '../utils/vsdcMock';
29: import ShiftManager from './ShiftManager';
30: import POSLayout from './ui/POSLayout';
31: import Modal from './ui/Modal';
32: import { useToast } from './ui/Toast';
33: import { useConfirm } from './ui/Confirm';
34:               // Create an empty held cart for this table immediately
35:               const emptyCartStr = JSON.stringify([]);
36:               const newCart = await window.api.addHeldCart({
37:                 name: tableName,
38:                 cartData: emptyCartStr,
39:                 waiterName: selectedWaiter?.name || currentUser.username
40:               });
41:               await loadHeldCarts();
42:               // Mock the cart array to bypass warning
43:               await restoreCart(newCart, [], () => true);
44:               setShowFloorPlan(false);
45:               setView('pos');
46:             }} 
47:             onClose={() => setShowFloorPlan(false)} 
48:           />
49:         </div>
50:       )}
51:     </>
52:   );
53: }
54: 
The above content shows the entire, complete file contents of the requested file.


=== CHUNK ===
Created At: 2026-07-13T22:23:33Z
Completed At: 2026-07-13T22:23:33Z
File Path: `file:///c:/Users/pc/Desktop/fidele/src/components/RestaurantPOS.tsx`
Total Lines: 1047
Total Bytes: 47578
Showing lines 525 to 570
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
525:           sdcId: vsdcResponse.data.sdcId,
526:           mrcNo: vsdcResponse.data.mrcNo,
527:           taxblAmtA,
528:           taxblAmtB,
529:           taxAmtB
530:         },
531:         {
532:           Cash: parseFloat(paymentDetails.Cash) || 0,
533:           Card: parseFloat(paymentDetails.Card) || 0,
534:           "Mobile Money": parseFloat(paymentDetails.Momo) || 0
535:         }
536:       );
537: 
538:       // 3. Print
539:       const printerName = await window.api.getSetting('receiptPrinter');
540:       const printResult = await window.api.printReceipt(htmlReceipt, printerName || '');
541:       
542:       if (printResult.success) {
543:         alert('Checkout complete and receipt printed!');
544:       } else {
545:         alert('Checkout complete, but printing failed: ' + (printResult.errorType || 'Unknown error'));
546:       }
547: 
548:       // 4. Clear cart and active order
549:       if (activeOrderId) {
550:         await window.api.deleteHeldCart(activeOrderId);
551:       }
552:       setCart([]);
553:       setCustomerName('');
554:       setNotes('');
555:       setPaymentMethod('Cash');
556:       setPaymentDetails({ Cash: 0, Card: 0, Momo: 0 });
557:       setShowPaymentModal(false);
558:       setActiveOrderId(null);
559:       setActiveOrderName('');
560:       setView('waiterSelect');
561:       setSelectedWaiter(null);
562:       loadData(); // refresh stock and tables
563:       if (onSave) onSave(); // tell App.jsx to reload sales
564: 
565:     } catch (err) {
566:       alert("Error during checkout: " + err.message);
567:     }
568:   };
569: 
570:   const filteredProducts = products.filter(p => {
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.
